const { Card, Typography } = require("@mui/material")



const Footer = (props) => {
    return (
        <Card sx={{ mt: '20px' }}>
            <Typography variant="h6">By Sarah Avins</Typography>
        </Card>
    )
}

export default Footer;